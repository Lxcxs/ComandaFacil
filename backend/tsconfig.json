{
  "compilerOptions": {
    /* Language and Environment */
    "target": "es2016",                             /* Define a versão do JavaScript para o código emitido. */
    "module": "commonjs",                           /* Define o sistema de módulos a ser usado. */
    "lib": ["es2016", "dom"],                       /* Inclui bibliotecas de definições de tipo para ES2016 e DOM. */
    "jsx": "preserve",                             /* Mantém o JSX como está no código. */
    "experimentalDecorators": true,                /* Habilita suporte para decoradores experimentais. */
    "emitDecoratorMetadata": true,                 /* Emite metadados de tipo para decoradores. */
    "esModuleInterop": true,                        /* Permite a interoperabilidade com módulos CommonJS. */
    "forceConsistentCasingInFileNames": true,       /* Garante que o casing dos nomes dos arquivos seja consistente. */

    /* Modules */
    "moduleResolution": "node",                     /* Define a resolução de módulos como 'node'. */
    "baseUrl": "./",                               /* Define o diretório base para resolução de módulos não-relativos. */
    "paths": {
      "*": ["node_modules/*"]                       /* Define caminhos de módulo personalizados. */
    },

    /* Emit */
    "outDir": "./dist",                            /* Define o diretório de saída para arquivos JavaScript. */
    "rootDir": "./src",                            /* Define o diretório raiz dos arquivos de origem TypeScript. */
    "sourceMap": true,                             /* Gera mapas de origem para arquivos JavaScript. */
    "declaration": true,                           /* Gera arquivos de declaração .d.ts. */
    "declarationMap": true,                        /* Gera mapas de declaração .d.ts. */
    "removeComments": true,                        /* Remove comentários nos arquivos emitidos. */
    "noEmitOnError": true,                         /* Não emite arquivos se houver erros de tipo. */

    /* Type Checking */
    "strict": true,                               /* Ativa todas as verificações de tipo estritas. */
    "skipLibCheck": true,                         /* Ignora a verificação de tipo nos arquivos de definição de tipo. */

    /* Project */
    "incremental": true,                          /* Habilita a compilação incremental. */
    "tsBuildInfoFile": "./.tsbuildinfo"            /* Especifica o caminho para o arquivo de informações de construção incremental. */
  },
  "include": ["src/**/*.ts"],                      /* Inclui todos os arquivos .ts na pasta src. */
  "exclude": ["node_modules", "dist"]              /* Exclui node_modules e dist da compilação. */
}

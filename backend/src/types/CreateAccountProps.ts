import { CreateAccountService } from "../services/CreateAccountService";

interface CreateAccountProps {
  name: string;
  email: string;
  password: string;
  table_amount: string;
}

export { CreateAccountProps };
